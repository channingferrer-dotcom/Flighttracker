[build]
builder = "nixpacks"

[deploy]
startCommand = "python -c 'from database import init_db; init_db()' && gunicorn app:app --workers 1 --threads 2 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[mounts]]
mountPath = "/data"
